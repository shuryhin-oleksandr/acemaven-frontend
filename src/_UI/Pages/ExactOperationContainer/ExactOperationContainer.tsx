import React from "react";
import Layout from "../../components/BaseLayout/Layout";
import OperationCard from "./OperationCard/OperationCard";

const ExactOperationContainer: React.FC = () => {
  return (
    <Layout>
      <OperationCard />
    </Layout>
  );
};

export default ExactOperationContainer;
