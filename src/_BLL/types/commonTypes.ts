export type VoidFunctionType = () => void
export type DateType = {from: string, to: string}

export enum AppCompaniesTypes {
    CLIENT = "client",
    AGENT = "agent"
}

export enum AppUserRolesType {
    AGENT = 'agent',
    BILLING = 'billing',
    MASTER = 'master'
}
